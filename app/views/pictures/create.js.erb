<% if @upload.new_record? %>
  alert('The photo could not be uploaded: <%= j @photo.errors.full_messages.join(', ').html_safe %>');
<% else %>
  // if ($('#uploadingPhotos') !== undefined) {
  $('#uploadingPhotos').append("<%=j render partial: 'pictures/picture', locals: { picture: @picture } %>");
  // }
<% end %>
